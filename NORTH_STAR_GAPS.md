# NORTH STAR GAPS — what more can be done (2026-07-14)

Lens: "does this make Alloy more founder-independent and more ownable?" Two adversarial audits (bus-factor successor test + acquirer tech/legal DD) over both repos + live box probes. Verdict: the product and GTM are ahead of the *substrate* — the company underneath the product is where the North Star leaks.

## THE TWO EXISTENTIAL FINDINGS

### 1. The data moat has ZERO backups (verified live) — **CLOSED at the logical-backup tier 2026-07-17:** nightly pg_dump systemd timer LIVE on the box (02:10 UTC, keep-14, Persistent=true) **+ off-box S3 tier LIVE since 2026-07-14** (write-only upload to versioned `s3://forj-db-backups-701275662474-eu-north-1-an`, role has Put+List only so the box can't destroy backups; `s3=ok` in backup.log nightly) **+ FULL restore drill passed 2026-07-17** (`/opt/backups/restore-drill.sh`: scratch-db restore in 31 s, 80/80 public tables, exact row parity incl. auth.users; receipt in `alloy-page/box/RESTORE.md`). After the verified drill, the 11 historical backup tables were dropped per data-audit D13. **Still open (Jacob, console):** DLM EBS snapshots for whole-volume protection; WAL archiving remains OFF (granularity = nightly).
No pg_dump anywhere on the box, no backup cron, WAL archiving OFF. The entire moat — 52,158 companies, 53,846 contacts, cloud verdicts, engagements, tenants, Smith's memory + KB (626 MB Postgres) — lives on ONE EC2 volume. `box-deploy.sh` auto-applies pushed SQL as postgres, so one bad migration is also a deletion vector. No document in either repo even names the risk. EBS-level snapshots unknown (my key can't see them by design — check AWS Backup/DLM in console).
**Fix (1 day + ~$5/mo):** nightly pg_dump → versioned cross-region S3 (instance-role bucket policy = Jacob's 5-min IAM action) + DLM EBS snapshots + WAL archiving + a RESTORE.md **with one tested restore drill** (the receipt matters as much as the backup). Interim same-day step needing no IAM: local-disk dump cron (87 GB free).

### 2. The IP chain of title runs through a customer
`alloy-page` (the product) lives in **Novalo-Technologies'** GitHub org; its **root commit (216578e) was authored by anders@novalo.se**; the box's GitOps deploy key and the Amplify source connection are registered in that org; the only governing document is the **non-binding** partnership termsheet with "IP assignment language" listed as an open counsel item. The strategy repo sits in the personal `jacobahmid-stack` account. No Forj GitHub org exists; Forj AB is mid-rename (privacy page: "Forj AB, org.nr 559019-9161, currently registered as Zmart Com West AB"); FORJ trademark unfiled.
**Why it matters:** at DD, "who owns the code" is question #1, and today's honest answer is "a customer's org, with an unsigned assignment." This is the difference between an acqui-hire priced on a salary and a tuck-in priced on the asset.

## THE REST OF THE RED LIST (ranked)
3. **Secrets incidents open:** AWS key pair rotation still pending (BACKLOG item 0), Smith ES256 signing key in plaintext in the working dir (gitignored only since acf2d65), Slack token on the rotation list. Trust page sells security posture; DD greps this in minutes. Fix: rotate all this week + key → Parameter Store + pre-commit secret scanner + a one-page incident/rotation log (documented closure impresses buyers).
4. **Trust page vs code:** trust.html says "Smith's AI reasoning runs on Amazon Bedrock in the EU," but claude-proxy routes ALL web_search tasks (find_contacts, research, techstack) + streamed chat to the Anthropic **US** API; Anthropic absent from the sub-processor list. Art 28/transparency defect against the flagship EU-moat claim. Fix: add Anthropic+SCCs to sub-processors and soften wording, OR route personal-data tasks Bedrock-only. Also reconcile "we do not scrape LinkedIn" with find_contacts' LinkedIn-URL hunting.
5. **Data provenance:** ~46.5k Vainu contacts — post-termination retention rights unverified in writing; top-1000 scraped around largestcompanies.com paywall; privacy page claims "legitimate interest (documented)" but no LIA/ROPA exists. Fix: pull contracts, write LIA + Art 30 ROPA (the /campaign legal work already scoped this), quarantine or re-derive the scraped rows via the free registry route.
6. **Revenue on no paper:** annual agreements unsigned (already the Activation-30D close); add DPA + **assignment/change-of-control clauses** so the ARR is transferable. Sign Novalo's annual paper AND the IP quitclaim in the same sitting — the pairing is the leverage.
7. **Bus factor = 1 on everything below the app:** successor test says a stranger can keep *shipping* (DEPLOY-BOX.md + GitOps are genuinely good) but cannot restore the DB, rebuild the box (no IaC/compose/Caddy/env in repo; HS256 gotchas memory-only), rotate the ~80 actual secrets (.env.example covers ~20), access any vendor account (no ACCOUNTS.md; recovery = Jacob's personal Gmail), respond to known incidents, or onboard tenant #3 (still a code-edit + redeploy).

## WHAT ALREADY IMPRESSES (the DD-positive column — protect these)
Eval harness with teeth (smith-eval/slop + offline scorer), 27 Vitest files + producer-locked icpScore, thoughtful RLS with documented leak-closures, claude_budget cost controls + NO_FALLBACK EU set, operable deploy runbooks, majority-free-registry data provenance by design, and the self-critical audit trail (AUDIT_2026-07-04, TRADEMARK_SCREEN) that reads like seller-run DD.

## 30-DAY CLEAN-ROOM SEQUENCE (dependencies honored)
- **W1 — stop the bleeding:** backups+restore drill; rotate all at-risk secrets + scanner + incident log; START entity completion (rename → Forj AB + VAT) since it gates everything legal.
- **W2 — own the name + founder's work:** file FORJ (PRV/EUTM, classes 9/35/42, per TRADEMARK_SCREEN) BEFORE Post #0 creates unprotected prior art; founder IP assignment Jacob → Forj AB; create Forj GitHub org + transfer the personal `alloy` repo (zero negotiation needed).
- **W3 — untangle Novalo (critical path):** termsheet → signed package: services agreement + **IP assignment/quitclaim** (covering root commit + inherited workflow) + license-back + co-sell terms; same meeting = both annual customer agreements w/ DPA + change-of-control; THEN transfer alloy-page → Forj org + re-point Amplify (transfer only AFTER the quitclaim signs).
- **W4 — data-room substance:** provenance register (per-source legal basis), LIA + ROPA + retention purge crons, Anthropic sub-processor fix, close security task #23, LICENSE/notice files (lockfile already GPL/AGPL-free), license-scan on file.

**End state day 30:** Forj AB owns both repos in its own org; signed IP chain from founder AND Novalo; FORJ filed; tested backups with a restore receipt; two annual contracts with assignment clauses; provenance + LIA/ROPA + incident log in one data-room folder.

## SPLIT OF LABOR
- **Claude can build now (authorization per item):** backup cron + RESTORE.md, BOX-REBUILD.md + commit compose/Caddy/env-template off the box, SECRETS.md (~80-var sweep from code truth), ACCOUNTS.md skeleton, INCIDENTS.md (six known failure modes as SSM one-liners), TENANT-ONBOARDING.md + wedge-config parameterization, EU routing/sub-processor fix, LIA/ROPA drafts, license notices, weekly live-state export cron.
- **Jacob only:** IAM bucket policy for backups (5 min), entity rename + VAT, trademark filing, lawyer engagement (founder assignment + Novalo package), GitHub org + repo transfers, secret rotations, vendor contract pulls, second break-glass admin, annual-paper signatures.
