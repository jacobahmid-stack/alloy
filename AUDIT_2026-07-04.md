Here is the deliverable.

---

# Forj / Alloy / Smith — Pre-Launch Audit + Roadmap
**2026-07-04 · for Jacob · written to be acted on Monday**

---

## State of forj.se + Alloy (verdict)

The site and app are genuinely close to launch-worthy: the free-read funnel works, the honesty rules are largely machine-enforced, and today's scoring-vocabulary and Norway work shipped clean. But three seams cut against the exact thing Forj sells — the Smith prompt still hard-codes **AWS-first displacement framing** the public FAQ forbids, the flagship public stat calls a **19.4k cloud-detection count "companies read"** (0 reads have actually been delivered), and every free-read funnel promises a **"workspace" that does not exist yet**. None are catastrophic and the launch is on deliberate hold, but these are precisely the credibility landmines a sharp cloud-partner founder or an acqui-hire diligence team would find first. Fix the handful below and the surface is coherent enough to post behind.

---

# PART 1 — AUDIT

23 verified findings. Grouped by severity. Line refs are to `C:/Users/jacob/alloy-landing/index.html` (forj.se) and `C:/Users/jacob/alloy-page/src/forge.jsx` (app) unless noted.

## ⛳ Fix-this-before-launch shortlist
The handful that actually gate a public post. Do these first; everything else can trail.

1. **Smith prompt AWS-first displacement** (`forge.jsx:4581/4586/4636/4647`) — contradicts your own FAQ and the pivot. Highest brand risk. *small*
2. **"companies read" overclaim** (`index.html:468/919`) — the single most challengeable stat on a REAL-proof-only page. *trivial*
3. **Workspace dead-end** (funnel-wide) — the read promises a workspace no visitor can open. *small*
4. **Partner-cloud defaults to AWS** (`index.html:502/846`) — the one interactive moment re-narrows every non-AWS visitor to AWS. *trivial*
5. **17 mangled Novalo names** (box data, e.g. `nn-sj.se` → "Sj", scored High fit) — a public teaser card titled "Sj"/"Hm" is an instant data-quality tell. *small*
6. **Rotate the committed signing key** (`smith-signing-key.json`, private `d` in-repo) — see Part 2; it inverts your trust claim. Load-bearing regardless of any bet. *small*

---

## HIGH

### H1 · Smith prompt hard-codes AWS-first displacement, contradicting the site's equal-clouds promise
- **Evidence:** `forge.jsx:4581` — *"frame Azure as the competitor cloud or a multi-cloud reality to migrate or co-exist with, never as a recommendation over AWS"*; `:4586` same for GCP; `:4636` and `:4647` both open *"Stay AWS-first."*; stale comments at 1073/1107/1120 confirm *"Smith stays AWS-first."* This is the literal "move them off their cloud" framing your public FAQ (`index.html:774`) forbids: *"AWS, Azure and Google Cloud, as equals... never frames a customer as one to move off their cloud."* This prompt writes customer-facing drafts.
- **Why it matters:** For a cloud-neutral product, leftover AWS bias in the draft engine is a credibility landmine — worst when the partner sells Azure/GCP.
- **Fix:** Delete the AWS-first clauses at 4581/4586/4636/4647 and the stale comments; replace with partner-cloud-aware framing where the partner's *configured* cloud drives the recommendation and other clouds are compared honestly, never framing the buyer as one to move off their existing cloud.
- **Effort:** small

### H2 · 17 mangled import rows are readable with 2-letter garbage names; 2 scored HOT
- **Evidence (live box data):** 17 rows `source='novalo-import-fixed'`, id prefix `nn-`, `name` = a 2-char subdomain fragment. `nn-sj.se` name "Sj" emp 3202 icp_score 72 (HOT); `nn-se.com` "Se"; `nn-hm.com` "Hm"; `nn-dk.nestle.com` "Dk". All pass the anvil floor in `smith-read` `classify()`, so any render path (public teaser, novalo workspace) shows a card titled "Sj" scored "High fit."
  - *Verify note:* the wider `nn-` set is larger than 17 (≈375 rows, ~20 HOT) with recognizable-but-unregistered names (Bufab, Skanska, Cloetta) that also resolve on the public read; the 17 two-char cases are the ugliest subset. `resolveCompany` returns these rows, so a search for "Bufab" renders the mangled card.
- **Fix:** Repair names from domain/orgnr to the real registry name (`sj.se` → SJ AB). **Do NOT blanket-archive as "duplicates"** — several (Bufab, Anticimex) are the *only* row for that org, so archiving would delete the sole record. Add a name-quality guard on import (reject ≤2-char / subdomain-fragment names).
- **Effort:** small

---

## MEDIUM

### M1 · TodayQueue is empty on the master lens (and its useMemo is defeated)
- **Evidence:** `forge.jsx:6725` `const proj = companies.filter((c) => c.project_id === project.id && isActiveCompany(c));` uses strict equality, not `inLens()`. On master `project.id==="master"`, but no row carries that id, so `proj` is empty — the founder's own default Today board (admins default to master, 11130) renders nothing. Dashboard/CompanyList/NBA all special-case master (8656/9193/8588); TodayQueue is the one that doesn't. Separately, `proj` is a fresh array each render feeding the `smithPicks` useMemo (6737-6739), defeating memoization.
- **Fix:** Mirror line 8656's master pattern (`c.project_id !== "demo"`), **not** raw `inLens` (which would wrongly include demo rows). Wrap `proj` in useMemo keyed on `[companies, project.id]`.
- **Effort:** small

### M2 · "companies read" counts detected library rows, not reads delivered
- **Evidence:** `index.html:468` renders `<b id="lb-co">…</b><span>companies read</span>`, filled at `:919` from the `forj_public_stats` RPC, which is literally `count(*) from active where cloud_ecosystem is not null` = ~19,430 (companies with a *detected cloud*). `forj_read_capture` holds exactly ONE row (kind=waitlist); **zero reads have ever been delivered.** "Read" is your machine-enforced core verb, and the CTA "Read a company free" makes a visitor parse it as the deliverable.
- **Fix:** Relabel to what it is — "companies analyzed" / "with a cloud detected." Reserve "reads" for the `forj_read_capture` sent count.
- **Effort:** trivial

### M3 · Duplicate-orgnr problem is worse than the backlog "dashed vs bare" note
- **Evidence (live box):** 592 normalized-orgnr groups have >1 row (1,192 rows); only **2** are the documented dashed-vs-bare case — **590** are identical-orgnr-string dups across different id/project, 527 spanning >1 project. Concrete: orgnr `556197-7033` "Fastighetssnabben Aktiebolag" = 3 rows across alto+novalo with **two different domains** (`fastighetssnabben.se` correct vs `gavlegardarna.se` wrong — a different entity), different clouds (azure vs other), scores 78/75/78, and the *wrong-domain* row is the one tagged "Tier 2 - Warm" (the actively-worked record). 169 groups have conflicting domains, 179 conflicting cloud verdicts. 1,165 of 1,192 dup rows are readable.
- **Why it matters:** The backlog frames this as benign formatting. It's cross-project record duplication with conflicting domains → same org reads two ways (one on a wrong domain = wrong stack/cloud on a public read), and it mis-routes internal work.
- **Fix:** Dedupe on normalized orgnr, keep the most-trusted domain (registry > vainu > import), merge `list_tags`/`claimed_by`, archive losers. Add a partial unique index on `replace(orgnr,'-','')` (the `idx_companies_orgnr_nodash` index already exists) to prevent re-introduction.
- **Effort:** medium

### M4 · Every free-read funnel promises a "workspace" that can't be opened
- **Evidence:** Funnel converges on a workspace as payoff: `index.html:506` *"The person and the full map open inside your workspace"*; `:883/:887` *"Real program math opens inside a workspace"* / "Unlock the full read"; `smith-read/index.ts:358` *"the full stakeholder map open inside a workspace."* Per `FORJ_ICP.md:84` Tier 2: *"Status today: WAITLIST… do not open to new tenants or self-serve… Nothing in this tier is buyable today and no document may imply it is."* Only reachable next step is the call or a canned demo — not the buyer's own workspace with the named person.
  - *Verify note:* the terminal surfaces already hedge honestly (`read.html:19` "when workspaces open", email "Join the workspace **waitlist**"). The defect is the **unhedged present-tense** promises at `index.html:506/:883` and email `:358`.
- **Fix:** Reframe those three to "opens when we set your workspace up together"; make the post-read primary CTA the call, waitlist secondary.
- **Effort:** small

---

## LOW

### L1 · SEO title/OG and the visible hero sell two different products
- **Evidence:** `index.html:8` `<title>` = "Co-sell & partner funding **platform**…" (Smith absent); `:444-445` hero = "Your AI **co-worker**… Smith forges the deal. You close." The `<title>`/description lead with the platform model; the on-page hero + OG/Twitter card lead with the Smith model. (OG and hero actually share the "Smith forges" spine, so it's title-vs-hero, not three framings.) No on-page contradiction — the description bridges both — the gap is confined to the search/tab headline.
- **Fix:** Pick one spine noun; make `<title>`, og:title, description, and h1 agree. e.g. `<title>` "Alloy by Forj — your AI co-worker for AWS, Azure & Google Cloud co-sell."
- **Effort:** small

### L2 · Proof strip and "How Smith runs" loop use eyebrow `<p>` as headers, no `<h2>`
- **Evidence:** `index.html:476` proof section's only heading is `<p class="eyebrow">`; `:567` loop intro same. Every other section (490,518,533,590,647,656,676,748,771,785) has a real `<h2>`. (The loop's 6 steps do use `<h3>`, so a screen-reader user still lands inside it; the proof strip has zero heading presence.)
- **Fix:** Add a real `<h2>` to both (visually-hidden if you want to keep the look): proof "Real pipeline since 1 June"; loop "How Smith runs, signal to meeting."
- **Effort:** trivial

### L3 · Rail nav "Loop" points at a section headed "How Smith runs"
- **Evidence:** `index.html:429` rail label "Loop" → `#loop`, whose visible kicker (`:567`) is "How Smith runs" — the word "Loop" never appears on screen, so scroll-spy lights "Loop" over a section titled otherwise. ("Alloy" rail item is fine — its h2 contains Alloy.) "Loop" is internal team vocabulary.
- **Fix:** Rename the rail label to "How it works", or surface "The loop · how Smith runs" in the kicker.
- **Effort:** trivial

### L4 · "Smith, the brain" SVG has a 640px min-width, horizontal-scrolls on mobile
- **Evidence:** `index.html:594-595` SVG `min-width:640px` inside an `overflow-x:auto` wrapper; at 375px it's ~1.9x viewport and must be side-scrolled. Handled correctly (page body doesn't scroll) but the single most conceptually central diagram is the one thing a mobile visitor fights to read.
- **Fix:** Add a <640px breakpoint that re-lays it vertically (inputs → Smith → outputs), same as the loopflow already collapses at 860px (`:340`).
- **Effort:** medium

### L5 · Proof band silently falls back to hardcoded 20/3/1 if the live RPC fails
- **Evidence:** `index.html:923` `var OFB={'pf-mtg':20,'pf-prop':3,'pf-win':1}` paints into the "Since we went live" band under copy "We publish these numbers because they are real" (`:483`), on RPC null/zero/timeout. Today the fallback *equals* the audited-real figures (verified live: RPC returns 20/3/1), so nothing untrue displays now — the risk is future drift.
- **Fix:** Drive the band solely from the live RPC (dash on failure), or keep the fallback pinned to current audited figures and refresh with reality.
- **Effort:** trivial

### L6 · CompanyList recomputes ~8 full-table filter passes on every keystroke
- **Evidence:** `forge.jsx:9212-9219` and `9350-9351` (missingDomain, uncheckedAws/Stack, counts.leads/booked/won, tab counts) are inline unmemoized full `projCompanies.filter()` scans, none depending on `query` or tab. The search box is controlled state, so typing re-runs all ~8; on master `projCompanies` ≈ 52k → ~416k redundant array visits per keystroke. (Adjacent heavier derivations *are* already memoized, so this was an oversight.)
- **Fix:** Wrap the 8 in `useMemo` keyed on `[projCompanies]`.
- **Effort:** small

### L7 · Dashboard `projStats` scans the full companies array per project, unmemoized — and is dead code
- **Evidence:** `forge.jsx:8789-8800` `projStats = projects.map(p => companies.filter(...))`, no useMemo, in a body that re-renders on drag/layout/hover. But `grep projStats` = one hit: **it's never consumed.**
- **Fix:** **Delete it** (not memoize dead code).
- **Effort:** small

### L8 · `funding_eligibility` fetched unbounded and refetched on every lens switch
- **Evidence:** `forge.jsx:8701` fetches with no Range/limit, in an effect keyed `[project.id]` that never uses `project.id` → identical full-table refetch on every lens change. Duplicate unbounded fetches at `:9204` and (undercounted by the finding) a third at `:11577`.
  - *Correction:* "cross-tenant" is **false** — RLS (`20260531210000_rls_funding_tables.sql`) scopes rows to the user's projects. It's merely unscoped + redundant. Table is 546 rows today, so no active truncation.
- **Fix:** Drop `project.id` from the dep (fetch once), share one `trackMap` across the 3 sites, add a Range/count guard.
- **Effort:** small

### L9 · `allCompanies` + `funding_eligibility` rely on the box PostgREST row cap being unlimited *(PLAUSIBLE)*
- **Evidence:** `forge.jsx:2522/8701/9204` issue reads with no Range and no limit; `sb()` sets no default. If the box ever set `PGRST_DB_MAX_ROWS`, the client would silently operate on a truncated slice with no error.
  - *Correction:* PostgREST's default is **unlimited** (the "1000 cap" is a Supabase-managed convention, not self-hosted). The known `select *` → statement-timeout gotcha proves no cap is applied today. Defensive hardening, not a live bug.
- **Fix:** Add a `Prefer: count` length assertion or paginate, so a future box misconfig can't silently truncate.
- **Effort:** medium

### L10 · 1,128 rows still carry legacy `icp_band`; retirement stalled
- **Evidence (box):** `icp_band` non-null: weak 641 / fair 406 / strong 45 / moderate 36 = 1,128; all `icp_at` on 2026-05-31 (nothing writes it since). `smith-read` `classify()` derives band from score and ignores `icp_band`, so the public read is safe — **but** `smith-brief/index.ts:210` still does `band = c.icp_band || …` and renders "score weak"/"score strong" into the Slack morning brief for any of these rows surfacing as a new lead.
- **Fix:** Point `smith-brief` off `icp_band`, then NULL/drop the column (only readers: `forge.jsx` CSV export + `smith-brief`).
- **Effort:** small

### L11 · 4,120 rows have no country; 591 are readable
- **Evidence (box):** country NULL/blank = 4,120; 1,348 domained; 591 pass the anvil floor. The teaser resolver has no country clause, so these render on a public card with no market attribution — outside the Sweden/Norway framing. (Finding's claim that the `nn-` junk rows overlap here is **false** — 0 `nn-` rows are null-country.)
- **Fix:** Backfill country from orgnr shape (10-digit SE vs 9-digit NO) or TLD; low urgency.
- **Effort:** small

### L12 · Two waitlist promises ("coverage grows" vs "workspaces open") write to one list
- **Evidence:** `index.html:896` success "We will tell you when **coverage grows**"; `read.html:19` "when **workspaces open**." Both POST `action:'waitlist'` into `forj_read_capture` kind=`waitlist` (`smith-read:854-865`) with **no field distinguishing the promise** — the eventual email can fulfill only one.
- **Fix:** Stamp a `reason` column (coverage vs workspace) or align the copy.
- **Effort:** trivial

### L13 · Free-read widget silently defaults `partner_cloud` to AWS
- **Evidence:** `index.html:502` AWS chip class `on` (Azure/GCP off); `:846` `var cloud="aws"`; a visitor who never touches the picker sends `partner_cloud:'aws'`, driving migrate target + program naming (`smith-read:294`). `FORJ_ICP.md:115`: *"leading every conversation with an AWS trigger quietly re-narrows the company to AWS, which the pivot forbids."* A written-rule violation at the one interactive moment.
- **Fix:** Require an explicit cloud selection before "Forge the read" enables, or neutralize the default so no cloud is privileged.
- **Effort:** trivial

---

## POLISH

### P1 · "Forge"/"funded play"/"software forges, you close" restated to dilution *(PLAUSIBLE)*
- **Evidence:** The motif recurs near-verbatim across h1 (445), hero-sub (446), Smith tag (535), loop intro (568), a dedicated stance band (647), footer (801) — 5-6 restatements; the standalone band at 644-650 largely restates the hero+loop. (Finding's line-list for "funded play" and the 30+ forge count are inflated — includes CSS class names and meta tags.)
- **Fix:** Keep the metaphor; cut the literal restatements. Fold the stance band's unique anti-autonomy idea into the FAQ or the Smith-brain section. Vary the 2nd/3rd "funded play."
- **Effort:** medium

### P2 · Smith proactive nudge dead on first load (stale-closure dep) *(PLAUSIBLE)*
- **Evidence:** `forge.jsx:11637-11648` effect deps `[activeProject, session]`; body reads `companies` but they resolve after the async load, so on first load the effect runs with empty companies and never re-runs.
  - *Correction:* only bites **admin/demo** (where resolved `activeProject` equals its initial "master"). For a real partner on their own lens, activeProject *changes*, the effect re-runs, nudge fires. Also this drives only a transient 7s launcher tooltip — **not** the LEAP-3 NbaStrip (prop-driven, unaffected). Overstated in the finding.
- **Fix:** Add `companies` (or a `hasData` flag) to the dep array, keep the once-per-open guard.
- **Effort:** trivial

### P3 · `EXPORT_COLUMNS` still lists retired `icp_band`
- **Evidence:** `forge.jsx:9636` includes `"icp_band"`. Still populated (selected at `:2521`), so exports ship real values today — the problem is forward-looking once the column empties.
- **Fix:** Drop it from `EXPORT_COLUMNS`, or add a derived band via `icpBand(icp_score)`.
- **Effort:** trivial

### P4 · A "not a fit" company still gets an "Unlock the full read" CTA
- **Evidence:** `classify()` returns `status:'ready'` for any floor-passing company regardless of band (`smith-read:299-313`), so a cold card reads "not a fit right now" (`index.html:872`) while the same card shows the "Unlock the full read" gate (`:887`). Mixed signal on a page whose pitch is "Smith won't waste you."
- **Fix:** For the cold band, soften the CTA — "Want the honest write-up anyway?" / "Or point Smith at a sharper account."
- **Effort:** trivial

---
---

# PART 2 — NEXT LEVEL

**Panel through-line:** Every best idea does the same thing — *move a unit of work that currently routes through Jacob's judgment or Jacob's phone into the machine, in a way that also becomes a transferable, aggregate-safe asset.* Two clusters dominate: **(1) founder-removal at the human-in-the-loop** (first paying seat → partner's own rep as approver → wedge onboarding as config, not code) and **(2) proof-as-byproduct** (a provenance layer + eval-gate certificate that auto-document "this ran without the founder and stayed trustworthy" for diligence). The free-read and the signed-identity one-liner are the near-zero-cost accelerants that make the funnel self-driving *inside* the launch hold.

## ⭐ The single sharpest move
**Graduate Alto or Novalo into the first paying self-sufficient seat — in forge-only mode, using the free-read as the self-driving demo, and the partner's own rep as the human.**

It is the only move that hits the **90-day goal** (first paying self-sufficient tenant) *and* proves **founder-independence** in one stroke: it converts Jacob-on-the-phones into the partner's-own-rep, using assets that already exist (the Desk relationship, the 3-workspace lens, the Tier-2 gate). It's a controlled one-tenant opening that doesn't break the launch gate. The only real gate is output clearing the "on point" bar — mitigate with forge-only mode (Smith drafts, rep sends). Idea-3 (free-read demo) and idea-2p1 (rep-as-approver) are the mechanism *inside* this move, not separate work.

---

## DO NOW (this week / bounded to days)

| # | Move | North-star tie / asset it leverages |
|---|------|-------------------------------------|
| **1** | **Graduate Alto or Novalo into the first paying Software seat** (forge-only) | Direct 90-day-goal hit; converts founder-on-phones → partner's rep. Assets: Desk relationship, 3-workspace lens, Tier-2 gate. |
| **2** | **Free-read-as-demo** — sequence every belief touch to end in "try one free read" | Highest leverage-per-effort; zero founder time (visitor drives it), respects the hold (no signup). The conversion rung idea-1 needs. |
| **3** | **Signed-identity trust wedge as the positioning spine** — one line Jacob repeats everywhere: "openly-AI, human sends everything, browser-verifiable" | Ownable category position that can't drift (canon-enforced). Near-zero build, arms every other motion. *Guardrail: `/smith` must verify green every time.* |

**Also do now, regardless of any bet:**
- **Rotate the committed private signing key.** `smith-signing-key.json` contains the private `d` parameter in-repo — it currently **inverts your trust claim into a diligence red flag.** Move it to KMS. This is the load-bearing security fix behind the entire signed-identity story (moat-4).
- **Merge the two ledger ideas** (Read Ledger + outcome ledger) into ONE provenance layer with two views before building either.

---

## DO NEXT (sequenced, weeks)

1. **Wedge-config as a product surface** (idea-6) — turn the hardcoded partner prompt string into a validated config row so partner #3 onboards **without a redeploy or Jacob**. *Pure founder-independence; precondition for the Software tier opening without him; leverages GitOps + the rulebook editor. Must validate configs against `scoring.js` floors and keep legal disqualifiers required. This makes seat #1 repeatable.*
2. **Partner-in-the-loop handoff** (idea-2p1) — route reply-agent/NBA/brief drafts to the partner's *named* seller as approver, same gate stack, Jacob off the button. *The mechanism behind seat #1. Feasibility docked: rubber-stamping risk means the canon/slop/faithfulness gate must be a hard, non-bypassable pre-send block.*
3. **The provenance layer** (merge Read Ledger + outcome ledger) — one aggregate-safe layer, two views: reads on one, outcome stages (`mote_bokat`/`forslag`/`vunnen`) on the other. *Makes the 20/3/1 claim recomputable and diligence-survivable; runs the FORJ_ICP-mandated retro to lock thresholds to evidence. Proof becomes a byproduct of the machine, not Jacob hand-counting.*
4. **The eval-gate certificate** (moat-5) — consolidate `smith-canon → smith-slop → smith-eval` (266 tests) into a documented output contract + a live faithfulness/voice pass-rate dashboard. *Answers the scariest acquisition question ("is this AI safe under our brand?"); the gate becomes the reviewer, not Jacob eyeballing drafts. Budget-cap the LLM judge (you have one).*
5. **Partnerships-job trigger inbound** (idea-2) — a firm hiring a partnerships manager is admitting the exact pain Forj removes; fire a targeted, human-sent read off the existing careers scanner. *Repeatable acquisition on a public trigger, not Jacob's network. On-brand for the hold. Trickle volume caps upside.*
6. **Prepare-pipeline as a standing radar** (idea-3p1) — a thin-data honest-miss auto-re-forges when the nightly detect ladder later resolves the cloud. *Self-healing top-of-funnel, zero founder retriage. Cap the watch window, reconfirm consent freshness, make it explicit opt-in.*
7. **Self-serve honest read as the waitlist Trojan horse** (idea-7p1) — let a waitlisted partner run the read engine against their own named territory, hard rate-capped, before a seat exists. *Bridges the hold and the 90-day goal; the concrete trial mechanism that de-risks conversion off the founder. Hard per-partner caps + named seller + Jacob approval per trial.*
8. **Partner-directory reverse targeting** (idea-7dist) — enumerate SE/NO firms tiered in the three partner directories, cross with Alloy firmographics → a ranked master queue. *Operationalizes the [DIRECTORY] ICP gate; makes idea-2 and any outreach sharper. Never claim a badge Forj doesn't hold when citing directory data.*

---

## BETS (high-upside, fund behind the do-now seat — never instead of it)

- **Nordic Cloud Verdict Graph as an ownable dataset** (moat-1) — freeze the 51,868-row corpus into a snapshotted, provenance-stamped asset with clear title: the thing a tuck-in buyer literally buys and can't lawfully rebuild fast (GDPR-clean, registry-derived). *GDPR posture must stay company/registry-facts-only; refresh must be funded or value decays.*
- **Funding-eligibility engine as codified cross-cloud IP** (moat-3) — extract `classify()` into a documented, tested, versioned module mapping a detected estate → the funded play, no-LLM (zero marginal cost). The single hardest asset for an acquirer to reproduce. *Don't overstate Azure/GCP parity — those branches are shallower than AWS.*
- **The Partner Fit Index** (scoring-benchmark) — productize the deterministic, cloud-fair, 266-test-locked `icpScore` as a named industry benchmark (like a credit score); makes Alloy the obvious place to act on the score. *Months; invites gaming; thin NO/DK coverage could mislead if quoted early. Lead with methodology transparency.*
- **Signed = compliant: the EU AI Act transparency wedge** (compliance-wedge) — sell the same signed asset as *compliance spend* (budget + regulatory urgency), re-rating the exit from GTM-tool acqui-hire to EU AI-governance asset. *Needs buyers not a coalition (easier than a standard); OneTrust-class incumbents could bolt on a disclosure SKU — stay narrow.*
- **The Signed Agent Standard / portable verifier** (merge moat-4) — generalize the passport into a portable spec + embeddable verifier widget. Highest ceiling, longest odds. *Standards die without a coalition; a solo founder gets ignored. De-risk by shipping the verifier widget as a product first and letting the EU AI Act pull adoption.*

**Park:** free-read viral loop (collides with the hold; a viral surface amplifies any redaction leak into a public hit on a partner's brand — their #1 fear), and the neutral partner-GTM MCP (depends on the assistant ecosystem maturing on someone else's timeline; harden `funding_fit`, park the rest).

**Drop:** signed-operators / placing Smith-equipped humans into partnerships roles — it's the staffing body-shop the thesis warns against; placements scale with Jacob-and-Qubad headcount, *toward* founder-dependence. Keep the Tribe application as a personal cash/positioning play only.

---

*Moonshots (verdict graph, funding engine, Partner Fit Index, signed standard) are the highest-ceiling ownability plays but trade near-term revenue for category position during a deliberate hold. Fund them as bets behind the do-now seat. The seat is what makes the 90-day clock move.*

---

## POST-AUDIT CORRECTIONS (verified by hand after the fleet, 2026-07-04)

1. **Shortlist item 6 ("rotate the committed signing key") is WRONG as stated.** Verified: `git ls-files` and `git log --all` show `smith-signing-key.json` was NEVER tracked or committed — no rotation needed. The REAL risk was structural: the private key sits untracked inside a git working directory with a GitHub remote. FIXED same day: `.gitignore` now excludes `smith-signing-key.json` + `forj-keys/` (committed). Optional hardening: move the key out of any repo directory entirely.
2. **Coverage gap:** the dedicated SECURITY audit lens crashed (structured-output failure) and produced no findings; the 23 verified findings come from the other 5 lenses. The June security audit + task #23 (anon-fn hardening) remain the standing security picture. A re-run of the security lens alone is cheap if wanted.
